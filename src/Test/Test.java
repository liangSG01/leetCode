package Test;

public class Test {
//    private static void quickSort(int[] arr, int left, int right) {
//        if (left < right) {
//             int pivot = partiotion(arr, low, high);
//             quickSort(arr, low, pivot - 1);
//             qucikSort(arr, pivot + 1, high);
//            }
//     private static int partition(int[] arr, int left, int right) {
//        int pivot = arr[left]; // 选第一个作为基准
//        int i = left;
//        int j = right;
//
//        while (i < j) {
//            // 从右边找第一个比pivot小的元素
//            while (i < j && arr[j] >= pivot) {
//                j--;
//            }
//            // 从左边找第一个比pivot大的元素
//            while (i < j && arr[i] <= pivot) {
//                i++;
//            }
//            // 找到后交换
//            if (i < j) {
//                swap(arr, i, j);
//            }
//        }
//
//        // 最后，把基准放到 i 的位置（即分界点）
//        swap(arr, left, i);
//
//        return i; // 返回基准位置
//    }
//        }
//    }
    public static void main(String[] args) {

    }
}
